#include "main.h"
#include <stdio.h>
/**
 * main - prints the largest prime factor of the number
 * Return: 0
 */
int main(void)
{
long x, maxf;
long n = 612852475143;
for (x = 1; x <= square; x++)
{
if (n % x == 0)
{
maxf = n / x;
}
}
printf("%ld\n", maxf);
return (0);
}
